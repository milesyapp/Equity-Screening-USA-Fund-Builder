# Pin major/minor versions to guarantee reproducibility across environments.
# Use `pip install -r requirements.txt` to install.
# Tested on Python 3.10+.
alpaca-py>=0.30,<1.0
yfinance>=0.2.40,<0.3.0
pandas>=2.1,<3.0
numpy>=1.24,<3.0
scipy>=1.11,<2.0
scikit-learn>=1.3,<2.0
lxml>=5.0,<6.0
requests>=2.31,<3.0
python-dotenv>=1.0,<2.0
# Quantum fund (QUBO portfolio selection). 'sim' sampler needs no token;
# 'hybrid'/'qpu' need DWAVE_API_TOKEN (see RESEARCH_LOGGING / QUANTUM_FUND docs).
dwave-ocean-sdk>=9.0,<10.0
